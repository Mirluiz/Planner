import React, { useEffect, useRef, useState } from "react";
import { Grid } from "@mui/material";

const BottomBar = () => {
  return (
    <Grid
      container
      sx={{
        background: "green",
      }}
    >
      Bottom
    </Grid>
  );
};

export { BottomBar };
