import React, { useEffect, useRef, useState } from "react";
import { Grid } from "@mui/material";

const Right = () => {
  return (
    <Grid
      container
      sx={{
        background: "red",
      }}
    >
      Right
    </Grid>
  );
};

export { Right };
